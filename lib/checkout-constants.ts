/** Default map center (Agdal, Rabat area). */
export const MAP_DEFAULT_CENTER = { lat: 34.0084, lng: -6.8539 };

/** Delivery zone circle (meters). */
export const DELIVERY_RADIUS_M = 5000;

/** Default delivery fee in MAD (shown when Livraison is selected). */
export const DEFAULT_DELIVERY_FEE_MAD = 15;
